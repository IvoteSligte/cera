
codebase:
    string pointer and length access
    null (pointer)
    explicit variable declaration types
    string slices
    debug info in object and assembly files
    integer type inference based on declared type (similar to `untyped integer` in Go)
    enums (without payloads)
    arrays
    type aliases
    explicit integer (and char) casts
    if-return, if-continue, if-break shorthands
    modules
    switch/match
    foreach element in array
    foreach element in custom type    
    tagged unions / enums with payloads
    implicit integer conversions (to larger integer types)
    generics
    remove VLAs
    equality operator for structs?
    make cross-platform (for starters, see "TODO: cross-platform" in code)
    unicode in chars/strings
    no naming conflicts between variables, types, and modules
    deduplicate string literals
    public/private globals
    cmake
    user installation directory (maybe ~/.local/share/cera or ~/.cera)
    statically link LLVM
    RAII?

warnings:
    unreachable code

tooling:
    standard library
    build system / package manager
    formatter

bugs:
    undefined symbol causes compiler panic in get_type_value
    lex/parse error messages are truncated
