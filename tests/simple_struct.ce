// OUTPUT: "true\n10\nthree\n"

struct Config {
    one: bool,
    two: int,
    three: str,
}
func main() {
    config := Config.{
        one: true,
        two: 10,
        three: "three",
    };
    print_bool(config.one); print_str("\n");
    print_int(config.two); print_str("\n");
    print_str(config.three); print_str("\n");
}
